[package]
name = "chamrisk-core"
version = "0.1.0"
edition = "2021"

[dependencies]
quick-xml = "0.31"
serde = { version = "1", features = ["derive"] }


[lib]
name = "chamrisk_core"
path = "src/lib.rs"

[dev-dependencies]
serde_json = "1"
